def run_tests(type: str, environment: str):
    print(f"✅ Running '{type}' tests on the '{environment}' environment")


def deploy_app(version: str, environment: str):
    print(f"🚀 Deploying version {version} to {environment}")


def generate_test_report(format: str):
    print(f"📄 Test report generated in {format} format")